package Exportar;

import Interfaces.Exportar;
import Mapa.Divisao;
import Missoes.Missao;
import Queue.LinkedQueue;

public class TesteExportar {
    public static void main(String[] args) {
        /*
        * LinkedQueue<LinkedQueue<Divisao>> simulacoes = new LinkedQueue<>();
        Missao missao = new Missao();
        // arranjar forma de guardar simulacoes em memória
        // se não o fizermos o nosso documento exportado só vai ter um trajeto
        simulacoes.enqueue(missao.);
        Exportar exportar = new ExportarDado(missao.getVersao(), simulacoes);
        System.out.println(exportar.exportarDados("C:/Testes/teste.txt");
        * */
    }
}
